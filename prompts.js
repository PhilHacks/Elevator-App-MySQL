const prompt = require("prompt-sync")();

const elevtorCall = prompt(
  "Call elevator by typing Call followed by the desired floor number."
);

const elevatorStatus = prompt("Get Elevator Status by typing Status");
